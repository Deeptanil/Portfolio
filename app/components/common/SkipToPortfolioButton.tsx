'use client';

// SkipToPortfolioButton is now rendered directly inside app/components/hero/index.tsx
// pinned to the title text group via Drei <Html transform />.
const SkipToPortfolioButton = () => {
  return null;
};

export default SkipToPortfolioButton;
