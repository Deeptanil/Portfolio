'use client';

import { Text } from '@react-three/drei';

const Work = () => {
  return (
    <group position={[0, 0, 0]}>
      <Text fontSize={0.5} color="white" position={[0, 0, 0]}>
        WORK & EDUCATION
      </Text>
    </group>
  );
};

export default Work;
